{
    "name": "public",
    "devDependencies": {
      "ajv": "^8.12.0",
      "ajv-cli": "^5.0.0",
      "jest": "^29.7.0",
      "ts-jest": "^29.1.1"
    },
    "scripts": {
      "test": "jest",
      "validate-ammunition": "ajv validate -s schemas/ammunition-schema.ts -d static/public/ammunition.json"
    },
    "jest": {
      "preset": "ts-jest",
      "testEnvironment": "node"
    }
  }
  