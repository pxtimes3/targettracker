import { TargetStore, type TargetStoreInterface } from "@/stores/TargetImageStore";
import { get } from 'svelte/store';

const targetStore = get(TargetStore);

function calculateReferenceValues()
{
    
}
